apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eventi-dtd.fullname" . }}
  labels:
    {{- include "eventi-dtd.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.app.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
