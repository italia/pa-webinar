apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventi-dtd.fullname" . }}
  labels:
    {{- include "eventi-dtd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eventi-dtd.selectorLabels" . | nindent 4 }}
