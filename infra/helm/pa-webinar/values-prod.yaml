# ─────────────────────────────────────────────────────────────
# pa-webinar — Generic production overrides
#
# For a specific cluster config, use values-production.yaml or
# create your own override file based on examples/values-full.yaml.
#
# Usage:
#   helm install videocall ./infra/helm/pa-webinar \
#     -f values.yaml -f values-prod.yaml -n videocall \
#     --set jitsi-meet.prosody.jwt.secret=$JITSI_JWT_SECRET
#
# The conference needs the same secret as JITSI_JWT_SECRET in the
# application Secret (see values-dev.yaml for the alternatives), and the
# internal XMPP passwords should be pinned: see
# jitsi.requirePinnedCredentials in values.yaml.
# ─────────────────────────────────────────────────────────────

app:
  replicaCount: 2
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: "2"
      memory: 1Gi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 8

podDisruptionBudget:
  enabled: true
  minAvailable: 1

# Use external database — no subchart
postgresql:
  enabled: false
  external:
    existingSecret: "videocall-secrets"
    secretKey: "DATABASE_URL"

jitsi:
  enabled: true

jitsi-meet:
  jvb:
    replicaCount: 0
