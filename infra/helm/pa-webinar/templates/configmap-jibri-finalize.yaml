{{- if and .Values.jitsi.enabled (index .Values "jitsi-meet" "jibri" "enabled") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pa-webinar.fullname" . }}-jibri-finalize
  labels:
    {{- include "pa-webinar.labels" . | nindent 4 }}
data:
  finalize.sh: |
    {{ .Files.Get "files/jibri-finalize.sh" | nindent 4 }}
{{- end }}
