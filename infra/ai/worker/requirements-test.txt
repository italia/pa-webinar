# Dipendenze MINIME per la suite pytest (logica pura).
#
# I moduli sotto test importano torch/whisperx/pyannote solo DENTRO le funzioni,
# non a livello di modulo: i test non li toccano. Restano solo le due leggere
# che compaiono in cima — numpy (align, xcorr, merge tracce) e httpx (il client
# del LLM). Cosi' il job CI non deve tirare ~2 GB di CUDA per verificare che
# l'allineamento dei sottotitoli non regredisca.
numpy<2
httpx==0.27.2
pytest
