{
  "name": "@pa-webinar/lobby",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated 2D top-down social lobby (Phaser) for PA Webinar — the pre-join waiting world. Mounted via mountLobby(); all I/O (presence, conference, schedule, devices) is injected behind ports.",
  "license": "EUPL-1.2",
  "type": "module",
  "exports": {
    ".": "./src/lobby/index.ts"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:lib": "vite build --mode lib",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mitt": "^3.0.1",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
